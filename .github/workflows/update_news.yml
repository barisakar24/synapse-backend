name: Update News (Final Fix)

on:
  schedule:
    - cron: '0 */3 * * *' # 3 saatte bir
  workflow_dispatch:

permissions:
  contents: write # İzin isteği

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: false # Standart kimliği kullanma
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install feedparser python-dateutil openai

      - name: Run News Generator
        env: 
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }} 
        run: |
          cd backend
          python news_generator.py

      - name: Commit and Push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name 'Synapse Bot'
          git config --global user.email 'bot@synapse.com'
          git add backend/news_data.json
          
          # Değişiklik varsa commit yap
          git commit -m "Haber Güncelleme: $(date)" || echo "Değişiklik yok"
          
          # İŞTE ÇÖZÜM: Token'ı linkin içine gömüyoruz
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git HEAD:master